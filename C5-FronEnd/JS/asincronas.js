setTimeout(() => {
    console.log('Esta instrucción se ejecuta después de 3 segundos, una sola vez');
}, 3000);

setInterval(() => {
    console.log('Esta instrucción se ejecuta cada 4 segundos');
}, 4000);